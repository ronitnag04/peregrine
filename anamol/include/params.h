#ifndef PARAMS_H
#define PARAMS_H

// params.h — umbrella header for backward compatibility.
// Includes param_types.h (manual infrastructure) and params_gen.h (generated data).
// Existing code that includes "params.h" continues to work unchanged.

#include "params_gen.h"  // transitively includes param_types.h

#endif  // PARAMS_H
